;
$.app = $.app || {};;
$.app.config = {
    /**
     * app configuration settings.
     * Runs before initializing everything.
     * 
     */
    setup: function() {
        /// <summary>
        ///     Setup all configuration.
        /// </summary>
    }
};